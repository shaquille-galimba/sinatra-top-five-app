<h2>Top five <%= @list.topic.name %> for <%= @list.user.username %></h2>

<ul>
	<% @list.arrayed_contents.each.with_index(1) do |content, index| %>
		<li><%= index %>. <%= content %></li>
	<% end %>
</ul>
